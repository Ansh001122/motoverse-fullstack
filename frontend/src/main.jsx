import React,{useEffect,useMemo,useState}from"react";import{createRoot}from"react-dom/client";import{createBooking,getBookings,getDashboardSummary,getVehicles,searchVehicles}from"./api";import"./styles.css";
function App(){const[vehicles,setVehicles]=useState([]),[bookings,setBookings]=useState([]),[summary,setSummary]=useState(null),[keyword,setKeyword]=useState(""),[category,setCategory]=useState("All"),[selectedVehicle,setSelectedVehicle]=useState(null),[form,setForm]=useState({customerName:"",customerEmail:"",rentalDays:1}),[message,setMessage]=useState("");async function loadData(){const[v,b,s]=await Promise.all([getVehicles(),getBookings(),getDashboardSummary()]);setVehicles(v);setBookings(b);setSummary(s)}useEffect(()=>{loadData().catch(()=>setMessage("Backend is not reachable. Start Spring Boot server first."))},[]);async function handleSearch(e){e.preventDefault();try{setVehicles(await searchVehicles(keyword))}catch{setMessage("Search failed. Please check backend connection.")}}async function handleBooking(e){e.preventDefault();if(!selectedVehicle){setMessage("Please select a vehicle first.");return}try{await createBooking({vehicleId:selectedVehicle.id,customerName:form.customerName,customerEmail:form.customerEmail,rentalDays:Number(form.rentalDays)});setMessage("Booking confirmed successfully.");setSelectedVehicle(null);setForm({customerName:"",customerEmail:"",rentalDays:1});await loadData()}catch(error){setMessage(error.response?.data?.error||"Booking failed.")}}const categories=useMemo(()=>["All",...new Set(vehicles.map(v=>v.category))],[vehicles]);const filteredVehicles=useMemo(()=>category==="All"?vehicles:vehicles.filter(v=>v.category===category),[vehicles,category]);return <main><section className="hero"><div><p className="eyebrow">Vehicle Rental Platform</p><h1>Motoverse</h1><p className="hero-text">Book bikes, scooters, cars, and SUVs with a clean full-stack rental workflow built using React and Spring Boot.</p><form className="search" onSubmit={handleSearch}><input value={keyword} onChange={e=>setKeyword(e.target.value)} placeholder="Search by vehicle, category, or city"/><button>Search</button></form></div><div className="hero-card"><span>Starting from</span><strong>₹450/day</strong><p>Instant demo booking with REST API integration.</p></div></section>{message&&<div className="alert">{message}</div>}<section className="stats"><Stat label="Total Vehicles" value={summary?.totalVehicles??0}/><Stat label="Available" value={summary?.availableVehicles??0}/><Stat label="Bookings" value={summary?.totalBookings??0}/><Stat label="Revenue" value={`₹${summary?.totalRevenue??0}`}/></section><section className="section-heading"><div><p className="eyebrow">Explore Fleet</p><h2>Available rentals</h2></div><select value={category} onChange={e=>setCategory(e.target.value)}>{categories.map(item=><option key={item}>{item}</option>)}</select></section><section className="grid">{filteredVehicles.map(vehicle=><article className="vehicle-card" key={vehicle.id}><img src={vehicle.imageUrl} alt={vehicle.name}/><div className="vehicle-body"><div className="vehicle-title"><h3>{vehicle.name}</h3><span className={vehicle.available?"available":"unavailable"}>{vehicle.available?"Available":"Booked"}</span></div><p>{vehicle.category} • {vehicle.location}</p><strong>₹{vehicle.pricePerDay}/day</strong><button disabled={!vehicle.available} onClick={()=>setSelectedVehicle(vehicle)}>Book Now</button></div></article>)}</section><section className="booking-panel"><div><p className="eyebrow">Booking</p><h2>{selectedVehicle?selectedVehicle.name:"Select a vehicle to book"}</h2>{selectedVehicle&&<p>Total estimate: ₹{Number(selectedVehicle.pricePerDay)*Number(form.rentalDays||1)}</p>}</div><form onSubmit={handleBooking}><input placeholder="Customer name" value={form.customerName} onChange={e=>setForm({...form,customerName:e.target.value})} required/><input placeholder="Customer email" type="email" value={form.customerEmail} onChange={e=>setForm({...form,customerEmail:e.target.value})} required/><input placeholder="Rental days" type="number" min="1" value={form.rentalDays} onChange={e=>setForm({...form,rentalDays:e.target.value})} required/><button>Confirm Booking</button></form></section><section className="bookings"><p className="eyebrow">Recent Bookings</p><h2>Booking history</h2>{bookings.length===0?<p>No bookings yet.</p>:<div className="booking-list">{bookings.map(booking=><div key={booking.id} className="booking-item"><strong>{booking.vehicle.name}</strong><span>{booking.customerName}</span><span>{booking.rentalDays} day(s)</span><span>₹{booking.totalAmount}</span></div>)}</div>}</section></main>}function Stat({label,value}){return <div className="stat-card"><span>{label}</span><strong>{value}</strong></div>}createRoot(document.getElementById("root")).render(<App/>);
